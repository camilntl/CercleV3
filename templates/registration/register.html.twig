{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	{# <h1>Register</h1>
				
				    {{ form_errors(registrationForm) }}
				
				    {{ form_start(registrationForm) }}
				        {{ form_row(registrationForm.firstName) }}
				        {{ form_row(registrationForm.lastName) }}
				        {{ form_row(registrationForm.email) }}
				        {{ form_row(registrationForm.plainPassword, {
				            label: 'Password'
				        }) }}
				        {{ form_row(registrationForm.agreeTerms) }}
				
				        <button type="submit" class="btn">Register</button>
				    {{ form_end(registrationForm) }} #}

<div class="w-screen flex place-content-center">
	<div class="px-10 space-y-8 h-screen flex flex-col justify-center max-w-lg">
		<div class="w-16">
			<img class="h-full" src="images/logo.png"/>
		</div>
		<div>
			<h2 class="text-3xl text-purple-500 mb-2 font-bold dark:text-purple-500">Bienvenue !</h2>
			<h2 class="text-3xl font-bold dark:text-purple-300">Créez votre compte</h2>
		</div>
		{{ form_errors(registrationForm) }}
		{{ form_start(registrationForm) }}

		<div class="space-y-6">
			<div>
				{# {{ form_row(registrationForm.lastName) }} #}
				{{ form_label(registrationForm.lastName, 'Nom', {'label_attr': {'class': 'block text-purple-700 mb-2 text-xs font-medium dark:text-purple-300'}}) }}
				{{ form_widget(registrationForm.lastName, {'attr': {'placeholder': 'Nom', 'class': 'border-none rounded-full w-full p-3 text-sm text-lavande-500 bg-purple-200 dark:bg-purple-300 focus:outline-none focus:shadow-outline placeholder-lavande-500 dark:bg-lavande-900 dark:border-lavande-900'}}) }}
			</div>

			<div>
				{# {{ form_row(registrationForm.firstName) }} #}
				{{ form_label(registrationForm.firstName, 'Prénom', {'label_attr': {'class': 'block text-purple-700 mb-2 text-xs font-medium dark:text-purple-300'}}) }}
				{{ form_widget(registrationForm.firstName, {'attr': {'placeholder': 'Prénom', 'class': 'border-none rounded-full w-full p-3 text-sm text-lavande-500 bg-purple-200 dark:bg-purple-300 focus:outline-none focus:shadow-outline placeholder-lavande-500 dark:bg-lavande-900 dark:border-lavande-900'}}) }}
			</div>

			<div>
				{# {{ form_row(registrationForm.email) }} #}
				{{ form_label(registrationForm.email, 'Email', {'label_attr': {'class': 'block text-purple-700 mb-2 text-xs font-medium dark:text-purple-300'}}) }}
				{{ form_widget(registrationForm.email, {'attr': {'placeholder': 'Email', 'class': 'border-none rounded-full w-full p-3 text-sm text-lavande-500 bg-purple-200 dark:bg-purple-300 focus:outline-none focus:shadow-outline placeholder-lavande-500 dark:bg-lavande-900 dark:border-lavande-900'}}) }}
			</div>

			<div>
				{# {{ form_row(registrationForm.plainPassword, {
																                label: 'Password'
																            }) }} #}
				{{ form_label(registrationForm.plainPassword, 'Mot de passe', {'label_attr': {'class': 'block text-purple-700 mb-2 text-xs font-medium dark:text-purple-300'}}) }}
				{{ form_widget(registrationForm.plainPassword, {'attr': {'placeholder': 'Mot de passe', 'class': 'border-none rounded-full w-full p-3 text-sm text-lavande-500 dark:bg-purple-300 bg-purple-200 focus:outline-none focus:shadow-outline placeholder-lavande-500 dark:bg-lavande-900 dark:border-lavande-900'}}) }}
			</div>
		</div>

		<div class="mt-4">
		{{ form_row(registrationForm.agreeTerms) }}
			<button type="submit" class="bg-purple-500 py-3 px-8 rounded-full text-base mb-4 w-full text-white">Inscrivez-vous</button>
			<p class="text-xs dark:text-purple-200">Vous avez déjà un compte ?
				<a href="{{ path('app_login') }}" class="text-purple-500">Connectez-vous</a>
			</p>
		</div>
		{{ form_end(registrationForm) }}
	</div>
	</div>
{% endblock %}
